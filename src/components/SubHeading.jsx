import React from 'react'

const SubHeading = ({text,className}) => {
  return (
    <h4 className={`text-center text-[50px] text-[#E5745D] font-voll font-medium ${className}`}>{text}</h4>
  )
}

export default SubHeading